const CenterIcon = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
};

const DatePickerWidth = {
  width: 150,
};

export const Styles = {
  CenterIcon,
  DatePickerWidth,
};
